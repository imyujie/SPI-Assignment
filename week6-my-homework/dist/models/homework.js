// Generated by LiveScript 1.3.1
(function(){
  var db, Schema, UserSchema;
  db = require('./db');
  Schema = db.mongoose.Schema;
  UserSchema = new Schema({
    requirementId: String,
    studentUsr: String,
    studentName: String,
    date: {
      type: Date,
      'default': Date.now
    },
    score: {
      type: Number,
      'default': -1
    }
  });
  UserSchema.virtual('score.tostring').get(function(){
    if (this.score >= 0) {
      return this.score;
    } else {
      return '尚未评分';
    }
  });
  module.exports = db.mongoose.model('Homework', UserSchema);
}).call(this);
